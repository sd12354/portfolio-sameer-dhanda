import { ReactNode } from 'react';
declare const useUpdateCursorify: () => {
    updateCursor: (cursor: ReactNode) => void;
    updateDelay: (delay: number) => void;
    updateOpacity: (opacity: number) => void;
    updateDefaultCursorVisible: (defaultCursorVisible: boolean) => void;
    updateEnabled: (enabled: boolean) => void;
};
export { useUpdateCursorify };
